#ifndef MQTT_DOT_H    /* This is an "include guard" */
#define MQTT_DOT_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/




void mqtt_main(void);

#endif
