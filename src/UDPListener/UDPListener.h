#ifndef UDP_LISTENER_DOT_H    /* This is an "include guard" */
#define UDP_LISTENER_DOT_H    /* prevents the file from being included twice. */
                              /* Including a header file twice causes all kinds */
                              /* of interesting problems.*/

void udp_listener_init();

#endif
