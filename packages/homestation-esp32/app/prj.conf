# Logging
CONFIG_LOG=y
CONFIG_LOG_BACKEND_UART=y

# ZBus
CONFIG_ZBUS=y

# UART (for RAK3172 AT commands)
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y
CONFIG_RING_BUFFER=y

# Device power management
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y

# Error handling
CONFIG_REBOOT=y
